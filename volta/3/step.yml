spec:
  outputs:
    node: {}
---
exec:
  command:
    - bash
    - -c
    - echo "Output file ${{ output_file }}" && (echo node=SOMETHING | tee ${{ output_file }}) && (echo "Ouput file content" && cat ${{ output_file }})