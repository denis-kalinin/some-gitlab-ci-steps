spec:
  outputs:
    node: {}
---
exec:
  command:
    - bash
    - -c
    - echo "Output file ${{ output_file }}" && (echo node=$(echo ~/.volta/bin/node) | tee ${{ output_file }}) && (echo "Ouput file content" && cat ${{ output_file }})