spec:
  inputs:
    volta_sh_url:
      type: string
      description: The URL to get Volta scipt, e.g. https://get.volta.sh (Optional)
      default: https://get.volta.sh
---
exec:
  command: 
    - bash
    - -c
    - curl ${{ inputs.volta_sh_url }} | bash

